<h1>参加者一覧ページ</h1>

<table border=1>
  <thead>
    <tr>
      <th>Userid</th>
    <!--  <th>Eventid</th>
      <th>Status</th>  -->
      <th>Comment</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @join_events.each do |join_event| %>

    
    <tr>
        <td><%= join_event.user_id %></td>
        <td><%= join_event.event_id %></td>
        <!--<td><%= join_event.status %></td>  -->
        <td><%= join_event.comment %></td>
        <td><%= link_to 'Show_', join_event %></td>
        <td><%= link_to 'Edit_', edit_join_event_path(join_event) %></td>

       <td><%= link_to 'Destroy_', join_event, method: :delete, data: { confirm: 'Are you shure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'このイベントに参加', new_join_event_path %>
<%= link_to 'Back', events_path %>
